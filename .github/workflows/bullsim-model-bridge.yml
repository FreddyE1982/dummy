name: BullSim Offline Dependency Bridge
on:
  pull_request:
  workflow_dispatch:
jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Download runtime wheels
        run: |
          mkdir wheels
          python -m pip download --only-binary=:all: --dest wheels onnxruntime tokenizers
      - name: Upload dependency artifact
        uses: actions/upload-artifact@v4
        with:
          name: bullsim-python313-runtime-wheels
          path: wheels/
          compression-level: 0
          retention-days: 7
